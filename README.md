## uv commands

Create new project

```shell
uv init mcp-server-code-of-conduct
```

Sync dependencies

```shell
uv sync
```

Add a new dependency

```shell
uv add fastmcp
```

```shell
uv add dotenv
```

Show dependency tree

```shell
uv tree
```
